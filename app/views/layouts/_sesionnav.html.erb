<p class="navbar-text pull-right">
<% if user_signed_in? %>
  Logeeado por <strong><%= current_user.email %></strong>.
  <%= link_to 'Editar Perfil', edit_user_registration_path, :class => 'btn btn-default navbar-btn' %>
  <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, :class => 'btn btn-default navbar-btn'  %>

<% end %>
